export const getField = state => state.app.field;

export const getCellsToClick = state => state.app.cellsToClick;

export const getDimention = state => state.app.inputValue;

export const getStartStatus = state => state.app.isStarted;

export const getFinishStatus = state => state.app.isFinished;
